name: secretfile
on:
  workflow_dispatch:

jobs:
  my_secrete:
    runs-on: ubuntu-latest
    - name: print out secrete
      run: |
        echo "MY_TOP_SEC : ${{secret.MY_TOP_SEC}}"
        echo "MY_TOP_SEC: ${{secret.i_don't_exist}}"